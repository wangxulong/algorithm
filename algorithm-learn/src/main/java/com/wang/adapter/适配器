适配器模式

定义：
    适配器模式将一个类的接口，转化为客户期望的另外一个接口。使得原本由于接口不兼容
    而不能一起工作的那些类可以在一起工作。

    client -> target -> adapter -> adaptee
适配器分类：
    1.组合：采用组合方式的适配器称为对象适配器
            特点：把“被适配者”作为一个对象组合到适配器类中，以修改目标接口包装被适配者。
    2.继承：采用继承方式称为类适配器
            特点：通过多继承不兼容接口，实现对目标接口的适配，单一的为某个类而实现适配。